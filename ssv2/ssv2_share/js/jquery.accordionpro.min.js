(function (a) {
    var b = function (b, c) {
        var d = {}, e = {}, f = {}, g = {}, h = {};
        d = {
            orientation: "horizontal",
            startClosed: !1,
            firstSlide: 1,
            theme: "basic",
            rounded: !1,
            rtl: !1,
            showSlideNumbers: !0,
            responsive: !0,
            scaleImages: !0,
            horizontalWidth: 1140,
            horizontalHeight: 300,
            verticalWidth: "100%",
            verticalHeight: 500,
            verticalSlideHeight: "fixed",
            activateOn: "click",
            touchEnabled: !0,
            onSlideOpen: function () {},
            onSlideClose: function () {},
            autoPlay: !1,
            cycleSpeed: 6e3,
            slideSpeed: 800,
            easing: "ease-in-out",
            pauseOnHover: !0,
            linkable: !1
        }, e = a.extend({}, d, c);
        var i = {
            w: 0,
            h: 0
        }, j = b.children("ol")
                .children("li"),
            k = {
                w: 0,
                h: 0,
                l: 0
            }, l = j.children(":first-child"),
            m = {
                w: 0,
                h: 48
            }, n = 0,
            o = 0,
            p = 0,
            q = e.orientation === "horizontal" ? 1 : 0,
            r = ["linear", "ease", "ease-in", "ease-out", "ease-in-out"],
            s = a.inArray(e.easing, r) >= 0 ? e.easing : d.easing,
            t = !q && e.verticalSlideHeight === "fitToContent" ? !0 : !1,
            u = e.theme === "transparent";
        return f.play = function (a) {
            var b;
            if(h.playing) return;
            b = h.nextSlide(a && a), h.playing = setTimeout(function () {
                l.eq(b())
                    .trigger("click.accordionPro")
            }, e.cycleSpeed)
        }, f.stop = function () {
            clearTimeout(h.playing), h.playing = 0
        }, f.next = function () {
            f.stop(), l.eq(h.currentSlide === k.length - 1 ? 0 : h.currentSlide + 1)
                .trigger("click.accordionPro")
        }, f.prev = function () {
            f.stop(), l.eq(h.currentSlide - 1)
                .trigger("click.accordionPro")
        }, f.destroy = function () {
            f.stop(), a(window)
                .off(".accordionPro"), b.off(".accordionPro")
                .removeData("accordionPro")
                .removeAttr("style")
                .removeClass("accordionPro horizontal vertical basic dark light stitch transparent rounded rtl closed responsive fitToContent scaleImages")
                .find("li > :first-child")
                .off(".accordionPro")
                .end()
                .find("b")
                .remove(), j.removeClass("slide selected")
                .removeAttr("style")
                .removeAttr("data-slide-name")
                .children()
                .removeAttr("style")
        }, f.debug = function () {
            return {
                elem: b,
                defaults: d,
                settings: e,
                methods: f,
                setup: g,
                core: h
            }
        }, g.styles = function () {
            b.outerWidth(q ? e.horizontalWidth : e.verticalWidth)
                .outerHeight(q ? e.horizontalHeight : e.verticalHeight)
                .addClass("accordionPro")
                .addClass(q ? "horizontal" : "vertical")
                .addClass(e.rounded && "rounded")
                .addClass(e.theme)
                .addClass(e.rtl && "rtl")
                .addClass(e.startClosed && "closed")
                .addClass(!q && t && "fitToContent")
                .addClass(e.scaleImages && "scaleImages"), j.addClass("slide"), k.l = j.length
        }, g.dimensions = function () {
            var a = j.eq(0)
                .children("div");
            i.w = b.width(), i.h = b.height(), o = b.outerHeight() - b.height(), p = parseInt(a.css("marginLeft"), 10) || parseInt(a.css("marginRight"), 10) ||
                parseInt(a.css("marginBottom"), 10) || 0, q ? n = parseInt(b.css("paddingLeft"), 10) + parseInt(b.css("paddingRight"), 10) : n = parseInt(b
                    .css("paddingTop"), 10) + parseInt(b.css("paddingBottom"), 10)
        }, g.getSlideCss = function (a, b) {
            var c = this,
                d = {
                    slide: {},
                    tab: {},
                    panel: {}
                };
            return q ? (k.w = i.w - k.l * m.h, k.h = i.h, d.slide = {
                width: k.w + m.h,
                height: "100%",
                position: {
                    left: a * m.h,
                    top: 0
                }
            }, d.tab.width = k.h, d.panel = u ? {
                width: k.w + m.h,
                height: k.h,
                position: {
                    left: 0,
                    top: 0
                }
            } : {
                width: k.w - p,
                height: k.h,
                position: {
                    left: m.h,
                    top: 0
                }
            }, e.rtl && (d.slide.position = {
                left: "auto",
                right: a * m.h,
                top: 0
            }, d.panel.position = u ? {
                left: "auto",
                right: 0 - p,
                top: 0
            } : {
                left: "auto",
                right: m.h - p,
                top: 0
            }), b.length && a > j.index(b) && (d.slide.position[e.rtl ? "right" : "left"] += k.w)) : (k.w = l.eq(0)
                .width(), k.h = i.h - k.l * m.h, t ? (d.panel.height = c.children("div")
                    .height(), d.slide.height = u ? d.panel.height : d.panel.height + m.h) : (d.slide.height = k.h + m.h, d.panel.height = u ? d.slide.height :
                    d.slide.height - m.h - p), d.panel.position = u ? {
                    top: 0,
                    left: 0
                } : {
                    top: m.h,
                    left: 0
                }, d.slide.position = {
                    top: a * m.h,
                    left: 0
                }, d.slide.width = d.tab.width = d.panel.width = "100%", b.length && a > j.index(b) && (t ? d.slide.position.top += b.height() - m.h : d.slide
                    .position.top += k.h)), d
        }, g.slidePositions = function () {
            var c = j.filter(".selected");
            !c.length && !e.startClosed && (j.eq(e.firstSlide - 1)
                .addClass("selected"), c = j.filter(".selected")), j.each(function (d) {
                var f = a(this),
                    h = g.getSlideCss.call(f, d, c),
                    i = f.children("h2"),
                    j = i.children("b");
                f.width(h.slide.width)
                    .height(h.slide.height)
                    .css(h.slide.position)
                    .attr("data-slide-name", b[0].id + "-slide-" + (d + 1))
                    .children("h2")
                    .width(h.tab.width)
                    .height(m.h)
                    .next()
                    .width(h.panel.width)
                    .height(h.panel.height)
                    .css(h.panel.position);
                if(e.showSlideNumbers) {
                    if(j.length) return;
                    i.append("<b>" + (d + 1) + "</b>")
                }
                else j.length && j.hide();
                b.hasClass("ie8") && b.hasClass("horizontal") && b.hasClass("rtl") && f.children("h2")
                    .css("marginRight", -(k.h - m.h))
            }), t && h.fitToContent(c)
        }, g.startClosed = function () {
            q ? b.css("width", k.l * m.h + o / 2 + n * 2 - 1) : b.css("height", k.l * m.h + o)
        }, g.events = function () {
            var c = 0;
            e.activateOn === "click" ? (l.on("click.accordionPro touchstart.accordionPro", h.animationCycle), e.startClosed && l.on(
                "click.accordionPro.closed touchstart.accordionPro.closed", h.startClosed)) : e.activateOn === "mouseover" && (l.on(
                "click.accordionPro touchstart.accordionPro mouseover.accordionPro", h.animationCycle), e.startClosed && l.on(
                "click.accordionPro.closed touchstart.accordionPro.closed mouseover.accordionPro.closed", h.startClosed)), Modernizr.touch && e.touchEnabled &&
                j.swipe({
                    left: function () {
                        q && (e.rtl ? h.currentSlide && f.prev() : f.next())
                    },
                    right: function () {
                        q && (e.rtl ? f.next() : h.currentSlide && f.prev())
                    },
                    up: function () {
                        q || f.next()
                    },
                    down: function () {
                        !q && h.currentSlide && f.prev()
                    },
                    threshold: {
                        x: 80,
                        y: 80
                    }
                }), e.pauseOnHover && e.autoPlay && b.on("mouseover.accordionPro", function () {
                    b.hasClass("closed") || h.playing && f.stop()
                })
                .on("mouseout.accordionPro", function () {
                    b.hasClass("closed") || !h.playing && f.play(h.currentSlide)
                }), e.linkable && a(window)
                .on("load.accordionPro hashchange.accordionPro", function (b) {
                    var c = j.filter(function () {
                        return a(this)
                            .attr("data-slide-name") === window.location.hash.split("#")[1]
                    });
                    c.length && h.animationCycle.call(c.children("h2")[0], b)
                }), q && e.responsive && (h.scale(), a(window)
                    .on("resize.accordionPro orientationchange.accordionPro", function () {
                        clearTimeout(c), c = setTimeout(function () {
                            h.scale()
                        }, 200)
                    }))
        }, g.ie = function () {
            var c = navigator.userAgent,
                d = c.indexOf("MSIE");
            if(d < 0) return;
            if(d !== -1) {
                c = c.slice(d + 5, d + 7), c = +c, c <= 7 && f.destroy();
                if(c >= 10) return;
                c === 8 && (q && (e.startClosed ? b.children("ol")
                    .css({
                        "min-width": k.l * m.h,
                        "min-height": e.horizontalHeight - o
                    }) : b.children("ol")
                    .css({
                        "min-width": e.horizontalWidth - o + n,
                        "min-height": e.horizontalHeight - o
                    })), j.each(function (b) {
                    a(this)
                        .addClass("slide-" + b)
                        .css({
                            "min-height": e.horizontalHeight - o
                        })
                })), b.addClass("ie ie" + c)
            }
        }, h.startClosed = function () {
            b.hasClass("closed") && (q ? (b.css("width", e.horizontalWidth), b.hasClass("ie8") && b.children("ol")
                    .css({
                        "min-width": e.horizontalWidth - o + n
                    })) : b.animate({
                    height: t ? (k.l - 1) * m.h + o + j.filter(".selected")
                        .height() : e.verticalHeight
                }, e.slideSpeed), b.removeClass("closed"), l.off("click.accordionPro.closed touchstart.accordionPro.closed mouseover.accordionPro.closed"),
                q && e.responsive && h.scale(), !e.startClosed && e.autoPlay && f.play())
        }, h.fitToContent = function (a) {
            b.hasClass("closed") || (b.animate({
                height: (k.l - 1) * m.h + o + a.height()
            }, e.slideSpeed), b.height((k.l - 1) * m.h + a.height()))
        }, h.scale = function () {
            var a = Math.min(b.parent()
                .outerWidth(!0) / e.horizontalWidth),
                c;
            a = Math.min(a, 1), b.hasClass("ie8") ? b.css("zoom", a) : (b.css(Modernizr.prefixed("transform"), "scale(" + a + ")"), q && b.css(
                "margin-bottom", -(e.horizontalHeight - e.horizontalHeight * a)
                .toFixed(2)))
        }, h.currentSlide = e.firstSlide - 1, h.previousSlide = h.currentSlide, h.nextSlide = function (a) {
            var b = a + 1 || h.currentSlide + 1;
            return function () {
                return b++ % k.l
            }
        }, h.playing = 0, h.animationFlag = !1, h.getSlidePosition = function (a, b) {
            var c = {};
            return typeof b == "number" ? q ? e.rtl ? c = {
                right: b + a * m.h
            } : c = {
                left: b + a * m.h
            } : c = {
                top: b + a * m.h
            } : q ? e.rtl ? c = {
                right: k.w + a * m.h
            } : c = {
                left: k.w + a * m.h
            } : c = {
                top: k.h + a * m.h
            }, c
        }, h.animationCycle = function (b) {
            var c = a(this),
                d = {
                    slide: c.parent(),
                    index: l.index(c)
                };
            d.next = d.slide.next(), d.prev = d.slide.prev(), h.previousSlide = h.currentSlide, h.currentSlide = d.index, h.animationFlag = !1, d.slide.hasClass(
                "selected") ? (e.onSlideOpen.call(d.prev.children("div")[0]), q ? (e.rtl && d.slide.position()
                    .left > i.w / 2 || d.slide.position()
                    .left < i.w / 2) && h.animateSlide.call(d) : t && d.index ? (h.animateGroup(d, !0), h.fitToContent(d.prev)) : d.slide.position()
                .top < i.h / 2 && h.animateSlide.call(d)) : (setTimeout(function () {
                e.onSlideOpen.call(d.slide.children("div")[0])
            }, e.slideSpeed), h.animateGroup(d), t && h.fitToContent(d.slide)), e.autoPlay && (f.stop(), f.play(l.index(j.filter(".selected"))))
        }, h.animateSlide = function (a) {
            var b;
            typeof this.position == "undefined" ? (this.position = h.getSlidePosition.call(this, this.index), this.index && j.removeClass("selected")
                .filter(this.prev)
                .addClass("selected")) : typeof this.position == "number" && (b = this.position, this.position = h.getSlidePosition(this.index, b)), this.index &&
                h.transition.call(this, a)
        }, h.animateGroup = function (b, c) {
            var d = ["left", "right"];
            a.each(d, function (d, e) {
                var f, g;
                d ? (c ? f = ":gt(" + (b.index - 1) + ")" : f = ":gt(" + b.index + ")", q ? g = k.w : t ? c ? g = b.prev.height() - m.h : g = b.slide.height() -
                    m.h : g = k.h) : (c ? f = ":lt(" + b.index + ")" : f = ":lt(" + (b.index + 1) + ")", g = 0), j.filter(f)
                    .each(function () {
                        var c = a(this),
                            d = {
                                slide: c,
                                index: j.index(c),
                                next: c.next(),
                                prev: c.prev(),
                                position: g
                            };
                        h.animateSlide.call(d, b)
                    })
            }), j.removeClass("selected")
                .filter(c ? b.prev : b.slide)
                .addClass("selected")
        }, h.transition = function (a) {
            var b = this;
            this.slide.stop(!0)
                .animate(this.position, e.slideSpeed, s, function () {
                    h.animationFlag || (e.onSlideClose.call(j.eq(h.previousSlide)
                        .children("div")), h.animationFlag = !0)
                })
        }, h.init = function () {
            if(typeof e.horizontalWidth != "number" || typeof e.horizontalHeight != "number" || typeof e.verticalHeight != "number") throw new Error(
                "horizontalWidth, horizontalHeight, and verticalHeight options must be integers.");
            b.hide(), t && (e.scaleImages = !1), e.linkable && (e.startClosed = !1), e.cycleSpeed < e.slideSpeed && (e.cycleSpeed = e.slideSpeed), g.styles(),
                imagesLoaded(b, function () {
                    g.dimensions(), g.ie(), b.show(), g.slidePositions(), g.events(), e.startClosed && g.startClosed()
                }), !e.startClosed && e.autoPlay && f.play()
        }, h.init(), f
    };
    a.fn.accordionPro = function (a) {
        var c = this,
            d = c.data("accordionPro");
        if(typeof a == "object" || !a) return c.each(function () {
            var e;
            if(d) return;
            e = new b(c, a), c.data("accordionPro", e)
        });
        if(typeof a == "string" && d[a]) return a === "debug" ? d[a].call(c) : (d[a].call(c), c)
    }
})(jQuery);